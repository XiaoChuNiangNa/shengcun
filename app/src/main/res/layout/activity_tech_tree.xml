<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <!-- 顶部导航栏：修复标题文字显示 -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="#1E299B">

        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:padding="12dp"
            android:src="@drawable/ic_left"
            android:background="?selectableItemBackgroundBorderless"
            android:contentDescription="返回"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <!-- 标题文字：关键修改 - 限制在返回按钮和希望点数之间，确保显示空间 -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="希望研究"
            android:textColor="@color/white"
            android:gravity="center"
            android:textSize="18sp"
            android:textStyle="bold"
            android:singleLine="true"
            android:ellipsize="end"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/btn_back"
            app:layout_constraintEnd_toStartOf="@id/tv_hope_points"
            app:layout_constraintHorizontal_bias="0.5"/>  <!-- 水平居中 -->

        <TextView
            android:id="@+id/tv_hope_points"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="希望点数：0"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:paddingHorizontal="16dp"
            android:singleLine="true"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- 标签页：保持可滑动模式 -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tab_layout"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:background="#F5F5F5"
        android:elevation="2dp"
        app:tabIndicatorColor="@color/accent"
        app:tabIndicatorHeight="3dp"
        app:tabSelectedTextColor="@color/accent"
        app:tabTextColor="@color/gray_dark"
        app:tabTextAppearance="@style/TechTabText"
        app:tabPaddingStart="16dp"
        app:tabPaddingEnd="16dp"
        app:tabMode="scrollable"/>  <!-- 确保标签可左右滑动 -->

    <!-- 内容区域：支持上下滑动 -->
    <androidx.viewpager.widget.ViewPager
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="16dp"
        android:paddingTop="12dp"/>
</LinearLayout>